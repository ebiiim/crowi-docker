# Icons

- To use your own icons, replace images to your own ones and set URLs to `ASSETS_ICON_FAV_*` environment variables in `/k8s/crowi.yml`.
- To use the default icons, delete `ASSETS_ICON_FAV_*` environment variables in `/k8s/crowi.yml`
- See `/run.sh` for details.
